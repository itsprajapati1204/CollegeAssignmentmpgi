// find the output of the code
#include <stdio.h>
int main()
{
    printf("%d", (-1<<1)<<2);
    getch();
    return 0;
}
// output - -8
//formual for +ve is number<<n =number*2ki power n ----for left shift
//formual for +ve is number>>n =number/2ki power n ----for rigth shift
